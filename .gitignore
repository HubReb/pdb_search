# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
env/

# Build / packaging
build/
dist/
*.egg-info/
*.egg

# uv
# (uv.lock is committed for reproducibility per pyproject [tool.uv] notes)

# Test / coverage caches
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Type / lint caches
.mypy_cache/
.ruff_cache/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project-local secrets — never commit
*.crypt
*.key
.env
.env.*
!.env.example

# Per-class log files generated by the legacy implementation
# (deleted in T026 but still produced during pre-modernization runs)
*.log
